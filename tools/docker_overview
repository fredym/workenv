#!/bin/bash

title()
{
  b=$(tput bold)
  n=$(tput sgr0)

  echo
  echo "$b$1$n"
  echo
}


title "HOSTS:"
docker-machine ls

title "IMAGES:"
docker images

title "VOLUMES:"
docker volume ls

title "NETWORKS:"
docker network ls

title "CONTAINERS:"
docker ps -a
